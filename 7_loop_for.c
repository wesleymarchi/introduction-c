#include <stdio.h>

int main() {
    int k;

    /* Inicializa o contador; expressão para continuar o loop; contador incrementado para o próximo loop */
    for (k = 0; k < 5; k++) {
      printf("%d\n", k);
    }

    return 0;
}